<UserControl x:Class="BillTime.Controls.MainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BillTime.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
            
        <!-- Row 0 -->
        <StackPanel Orientation="Vertical" Grid.Row="0">
            <TextBlock>Client</TextBlock>
            <ComboBox x:Name="clientDropDown" />
        </StackPanel>
    
        <!-- Row 1 -->
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <StackPanel Orientation="Vertical">
                <TextBlock>Hours</TextBlock>
                <TextBlock x:Name="hoursTextbox"/>
            </StackPanel>
            <Button x:Name="operateTime">Start Timer</Button>
        </StackPanel>

        <!-- Row 2 -->


    </Grid>
</UserControl>
